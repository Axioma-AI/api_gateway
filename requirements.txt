fastapi==0.115.14          # Framework ASGI de alto rendimiento
uvicorn==0.35.0            # Servidor ASGI recomendado para FastAPI
starlette==0.46.0          # Capa ASGI subyacente de FastAPI (requerida por FastAPI)
typing-extensions==4.14.0  # Extensiones de typing (requerida por FastAPI/Pydantic)
click>=7.0                 # Interfaz de línea de comandos (requerida por uvicorn)
h11>=0.8                   # Protocolo HTTP/1.1 (requerida por uvicorn)
sniffio>=1.1               # Detección de bibliotecas asíncronas (requerida por anyio)
anyio>=3.6.2,<5            # Biblioteca de concurrencia asíncrona (requerida por starlette)
annotated-types>=0.6.0     # Tipos anotados (requerida por pydantic)
typing-inspection>=0.4.0   # Inspección de tipos (requerida por pydantic)
pydantic-core==2.33.2      # Núcleo de pydantic (requerida por pydantic)
pydantic==2.11.7           # Validación de datos basada en type hints
pydantic-settings==2.10.1  # Gestión de configuración y .env
certifi                    # Certificados SSL (requerida por httpx)
httpcore>=1.0.0,<2.0.0     # Núcleo HTTP (requerida por httpx)
idna                       # Nombres de dominio internacionalizados (requerida por httpx)
httpx==0.28.1              # Cliente HTTP asíncrono
coloredlogs==15.0.1        # Logs en color en la consola
python-dotenv>=0.21.0
humanfriendly>=9.1
